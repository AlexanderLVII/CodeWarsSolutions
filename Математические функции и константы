Математические функции и константы






1.   Discover The Original Price 
function discoverOriginalPrice(discountedPrice, salePercentage) {
  return +(discountedPrice * 100 / (100 - salePercentage)).toFixed(2);
}


2.  Formatting decimal places #0 
// https://www.codewars.com/kata/formatting-decimal-places-number-0
	function twoDecimalPlaces(n) {
 return +n.toFixed(2)
}

3.  Formatting decimal places #1 
function twoDecimalPlaces(number) {
  return Math.trunc(number*100)/100
}

4.   Find the Square Root 
function squareRoot(x) {
 return Number((x ** 0.5).toFixed(5))
}

5.  Area of a Square 
	Длина дуги = L = pi*R/180*alpha
	Alpha – это угол от центра к концам дуги. У нас = 90
	Находим r = 2 * L / pi
function squareArea(A){
  return +(Math.pow(2 * A / Math.PI, 2)).toFixed(2)
}

6.  How many times should I go? 
function howManyTimes(annualPrice, individualPrice) {
 return Math.ceil(annualPrice / individualPrice)
}

7. Return the closest number multiple of 10 
const closestMultiple10 = num => {
  return Math.round(num / 10) * 10;
};

8. Keep Hydrated! 
function litres(time) {
  return Math.floor(time * 0.5);
}


9. Count Odd Numbers below n 
function oddCount(n){
   return Math.floor(n / 2)
}

10.   Century From Year 
function century(year) {
   return Math.ceil(year / 100);
}
